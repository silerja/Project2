//import code for quiz to test
var quiz = require("..index");

describe("The answer to the first question 'Who is this?'.")
	if( choice = Joe Siler) {
		greeting = "Correct";
	} else {
		greeting = "Incorrect";
	};
